
import {FormControl, FormGroup, Validators, FormBuilder } from '@angular/forms'

export class UserForm extends FormGroup {

// firstName : FormControl<string>('', Validators.required);
// lastName : FormControl<string>('', Validators.required);
// email : FormControl<string>('', Validators.required);
// address : FormControl<string>('', Validators.required);
// apt : FormControl<number>(0);
// city : FormControl<string>('', Validators.required);
// state : FormControl<string>('', Validators.required);
// zipCode : FormControl<number>(0, Validators.required);
// gender : FormControl<string>('');
// dob : FormControl<string>('', Validators.required);
// phone : FormControl<number>(0, Validators.required);
//
// contructor(builder: FormBuilder){
//   super(UserForm);
//
// }

}
